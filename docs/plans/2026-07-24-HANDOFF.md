# browserid-bsky — session handoff (2026-07-24)

Continue the Bluesky bridge work. Everything below is **live in
production**, committed, and green. Nothing is half-built.

## TL;DR — what exists and works

A browserid-ng identity can provision a real Bluesky account and let a
warranted agent post to the live network, with **cryptographically
unforgeable, publicly verifiable provenance** and a **client-rendered
labeler** as the trust surface. Demonstrated end-to-end on bsky.app.

The one-liner value prop, proven live: *scoped, revocable, attributable
agent access to atproto that no other account can forge.*

## Two repos

- **`vthunder/browserid-ng`** (`~/src/browserid-ng`) — the protocol +
  reference impl (broker/registrar/core/rp/agent). Stays bsky-free.
- **`vthunder/browserid-bsky`** (`~/src/browserid-bsky`) — THIS repo, all
  bsky work. Depends on `browserid-core`/`browserid-rp`/`browserid-agent`
  as **git deps pinned by Cargo.lock** (bump with
  `cargo update -p browserid-core -p browserid-rp -p browserid-agent`).
  Workspace members: `pds-bridge` (the service), `smoke` (test driver).

## Deployment (all on sandmill.org dokku)

- **`bsky-bridge`** → `https://bsky.browserid.me` — the Rust bridge (also
  the labeler). Deployed by CI: `.github/workflows/deploy-bridge.yml` builds
  the image → GHCR (`ghcr.io/vthunder/browserid-bsky/pds-bridge`, public) →
  `dokku git:from-image`. Needs repo var `DOKKU_HOST=sandmill.org` + secret
  `DOKKU_SSH_KEY` (a dedicated key authorized as dokku ssh-key
  `browserid-bsky-ci`). Push to main = deploy.
- **`bsky-pds`** → `https://pds.bsky.browserid.me` — stock
  `ghcr.io/bluesky-social/pds:0.4`.
- Host IP `198.199.110.160`. One **SAN TLS cert** covers
  `pds.bsky.browserid.me` + `*.at.browserid.me`, auto-renewing via acme.sh
  deSEC **alias-mode DNS-01** (`/root/.acme.sh/deploy-bsky-pds.sh` reload
  hook → `dokku certs:add bsky-pds`). Runbook + cert details:
  `docs/plans/2026-07-24-handle-verification-wildcard-cert.md`.

### bsky-bridge env (dokku config)
`BRIDGE_ORIGIN=https://bsky.browserid.me`, `HANDLE_DOMAIN=at.browserid.me`,
`BROKER_URL=https://browserid.me`, `PDS_URL=https://pds.bsky.browserid.me`,
`PDS_ADMIN_PASSWORD=<same as bsky-pds>`,
`LABELER_K256_PRIVATE_KEY_HEX=<k256 scalar>` (set — enables the labeler).

## The demo account

- DID `did:plc:c4arwdso2jwuyb4fa5noxx5w`, handle `claude.at.browserid.me`
  (verified on the network), on our PDS.
- Provisioned by `danmills+bluesky@sandmill.org` (a subaddress of
  `danmills@sandmill.org`, sandmill.org is a primary IdP). All demo posts so
  far are **as-itself** (grantor==grantee). The **on-behalf** path
  (grantor≠grantee) is code-supported and covered by tests but not yet run
  live.

## Driving it (smoke tool)

```sh
cd ~/src/browserid-bsky
BRIDGE_IP=198.199.110.160 cargo run -q -p smoke -- setup [handle]   # provision (needs approval)
BRIDGE_IP=198.199.110.160 cargo run -q -p smoke -- post "text"      # attested post via /browserid/post
```
State in `smoke-state.json` (credential + grants + did). `BRIDGE_IP` pins
DNS to dodge negative-cache lag. The agent side is driven via the
`@browserid-ng/wallet` MCP for the human-approval steps, or the smoke tool's
`request_provision`.

## Architecture (how the bridge works)

- **`/browserid/provision`** — browserid login → creates the PDS account +
  binds grantor-email→DID. First-party only (grantee==grantor). Password
  shown once (see open bean `azxl`).
- **`/browserid/token`** — RFC 7521: four-object bundle → `bidb_` bearer
  token. **Verification is OUTSOURCED** to the hosted verifier
  (`POST {broker}/verify-access`) — one algorithm; the bridge is a plain RP.
  Token bound to {did, grantor, grantee, holder, scopes, warrant-status},
  1h TTL. Warrant status re-checked fail-closed on every use.
- **`/browserid/post`** — phase-2 attested post: grantee signs an
  attestation (access key), bridge verifies + reserves nonce (replay) +
  writes the post + provenance.
- **`/xrpc/*`** — bridge-token traffic is scope-enforced (allowlist) and
  repo-pinned, then forwarded with the account session; everything else
  passes through to the PDS. (No websocket/firehose passthrough yet.)
- **`/verify?uri=|n=`** — public receipt (HTML + `&format=json`); embeds the
  post; anti-spoof check. Resolves any did:plc.
- **`/.well-known/did.json` + `/xrpc/com.atproto.label.queryLabels`** — the
  labeler.

Provenance records (bean 27c0, DONE): `me.browserid.warrant` (once per
warrant, deduped) + `me.browserid.provenance` (per post, {post, warrant-ref,
attributedTo=grantor, executedBy=grantee, nonce, attestation}). Lexicon doc:
`docs/lexicons/me.browserid.provenance.md`.

## Security boundary (important framing)

The in-post `🔗 verify` facet link is **convenience, not security** — a
malicious post can link to a fake verifier. The **labeler** (client-rendered
by bsky.app, keyed on the real post) is the trust surface; the paste-a-link
page is the manual fallback. See bean `4zx7`.

## DONE this session

- Bridge P1 (provision/exchange/scoped proxy/revocation), deployed.
- Handle verification live (`*.at.browserid.me`, SAN cert, auto-renew).
- Provenance phase 1 (`27c0`) + phase 2 unforgeable attestations (`n78o`).
- Public verifier (`rjpk`) — nonce/at-uri/bsky.app-URL, embeds post,
  anti-spoof.
- In-post link as a clickable richtext facet.
- **Labeler milestone A (`4zx7`)**: `did:web:bsky.browserid.me`, k256
  signing (spec-correct: DAG-CBOR→SHA-256→k256 low-S 64-byte compact, sig as
  `$bytes`), `did.json` + `queryLabels` live, emitting a signed
  `browserid-verified` label for the demo post. Crypto unit-tested.

## NEXT: labeler milestone B → C

**Decision made:** ONE labeler; label VOCABULARY = 3 values (one labeler can
emit many, chosen per post):
- `browserid-verified` (as-itself), `browserid-on-behalf` (delegate),
  `browserid-broken` (provenance present but failed). All `inform` for v1
  (broken→alert once trusted). **No-sidecar = no label** (absence is the
  signal; labeling the whole network is infeasible).

**Milestone B (build next):** make the labeler a **subscribable account** so
bsky.app's moderation UI shows it:
1. Create a labeler account (did:plc on our PDS, e.g. handle
   `labeler.at.browserid.me` or similar).
2. Publish an `app.bsky.labeler.service` record in its repo declaring the
   label values + `labelValueDefinitions` (name/description/severity/blur).
3. **The fiddly/uncertain bit:** add `#atproto_label` (the k256 key) +
   `#atproto_labeler` service to that account's **did:plc document via a PLC
   operation**. (Milestone A used did:web served by the bridge; bsky.app's
   subscribe UI likely wants a did:plc account with the service record.
   OPEN: try subscribing to the did:web labeler first — if bsky.app accepts
   it, B shrinks a lot.)
4. Point the labeler's key material so the SAME k256 key is in the account's
   DID doc AND used by the bridge to sign (env `LABELER_K256_PRIVATE_KEY_HEX`).

**Milestone C:** Dan subscribes to the labeler in bsky.app (Settings →
Moderation) and we confirm the `browserid-verified` badge renders on the
demo post. Then add `on-behalf` (trivial) and `broken` (needs a
`subscribeLabels` firehose consumer — queryLabels only covers posts in view;
broken wants proactive firehose eval of `me.browserid.provenance` records).

Start B by testing whether bsky.app will subscribe to the existing did:web
labeler — cheapest way to learn if the did:plc account is even needed.

## Other open beans

**browserid-bsky:** `azxl` (disable PDS password login — last operator-trust
hole, high), `htjb` (end-to-end setup guide for any browserid email, both
provenance paths), plus a bridge DB migration story (currently an idempotent
ALTER hack in store.rs).

**browserid-ng (protocol):** `kozn` (extract ONE verifier crate so
rp/broker/bridge share the DNSSEC-rooted algorithm — also fixes the
bridge/labeler's advisory `.well-known` IdP checks), `44jm` (rename
`/verify-access`→`/verify`), `i9rr` (broker verifier cross-issuer parity —
doesn't block the bridge since it verifies via core), `68av` (mint jti
replay), `7ydv` (mint doesn't check device revocation — a removed device
still mints), `ga3w` (spec rewrite to holder/grantor-grantee model),
`4lxl` DONE but the **deployed browser.me broker predates it** — redeploy so
`/verify-access` enforces fail-closed status (gate on mingo publishing its
status list). Audit: `~/src/browserid-ng/docs/plans/2026-07-23-spec-code-divergence-audit.md`.

## Gotchas (all bit us this session)

- **DNS negative caching**: new subdomains NXDOMAIN-cache; use `BRIDGE_IP=`
  pin, or wait / flush. Records are all live now.
- **dokku = one cert bundle per app** → must be a SAN cert covering all the
  app's vhosts (broke pds.bsky when we installed a wildcard-only cert).
- **`CREATE TABLE IF NOT EXISTS` won't add columns** → store.rs has an
  idempotent `ALTER TABLE … ADD COLUMN` migration; add new columns there.
- **PDS expired session** = HTTP **400 `ExpiredToken`** (not 401); refresh
  on both, and update the LOCAL account copy after refresh (provenance
  writes use it).
- **axum `Query` can't decode repeated params** (`uriPatterns`) → parse the
  raw query.
- **k256 0.14**: compressed pubkey via `verifying_key().to_sec1_bytes()`;
  `Signature::normalize_s()` returns a `Signature` (not Option).
- Verify locally before pushing: `cargo test -p pds-bridge` (14 tests) +
  `cargo build -p smoke`. Every push to main deploys.
